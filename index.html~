---
layout: default
---

<div class="home">

  <h1 class="page-heading"></h1>

  <ul class="post-list">
    {% for post in site.posts %}
      <li>
        <span class="post-meta"><span class="date">{{ post.date | date: "%b %-d, %Y" }}</span></span>

        <h2>
          <a class="post-link post-title" href="{{ post.url | prepend: site.baseurl }}">{{ post.title }}</a>
        </h2>
        {{ post.excerpt }}<!--a href="{{ post.url | prepend: site.baseurl }}"> ادامه مطلب </a-->
      </li>
    {% endfor %}
  </ul>

  <!-- moved to page footer -->
  <!--p class="rss-subscribe">subscribe <a href="{{ "/feed.xml" | prepend: site.baseurl }}">via RSS</a></p-->

</div>
